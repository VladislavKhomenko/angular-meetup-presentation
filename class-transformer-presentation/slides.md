---
theme: seriph
layout: image
image: /backgrounds/media-image1.png
title: DTO без боли
info: |
  ## DTO без боли: как class-transformer упрощает жизнь
  
  Презентация о том, как использовать class-transformer для работы с DTO в TypeScript приложениях.
  
  Подходит для Angular, React, Node.js и других TypeScript проектов. Показываем реальные примеры из production проектов.
class: text-center
drawings:
  persist: false
transition: slide-up
mdc: true
colorSchema: light
seoMeta:
  ogImage: auto
colors:
  primary: '#C3002F'
  secondary: '#1976D2'
  accent: '#FF6600'
---

<style>
/* Принудительная светлая тема */
html {
  color-scheme: light !important;
}

html.dark,
html[data-theme="dark"] {
  color-scheme: light !important;
}

/* Глобальные стили для лучшего отображения */
.slidev-layout {
  font-size: 0.9rem;
}

.slidev-layout h1 {
  font-size: 2.5rem;
  margin-bottom: 1rem;
}

.slidev-layout h2 {
  font-size: 1.8rem;
  margin-bottom: 0.8rem;
}

.slidev-layout h3 {
  font-size: 1.4rem;
  margin-bottom: 0.6rem;
}

.slidev-layout pre {
  font-size: 0.8rem;
  line-height: 1.4;
}

.slidev-layout .grid {
  gap: 1rem;
}

.slidev-layout .mt-4 {
  margin-top: 0.5rem;
}

.slidev-layout .mt-8 {
  margin-top: 1rem;
}

.slidev-layout .p-3 {
  padding: 0.5rem;
}

.slidev-layout .p-4 {
  padding: 0.75rem;
}

.slidev-layout .text-sm {
  font-size: 0.8rem;
}

/* Стили из PPTX шаблона */
:root {
  --color-primary: #C3002F;
  --color-secondary: #1976D2;
  --color-accent: #FF6600;
  --color-dark: #1a1a1a;
  --color-light: #ffffff;

  * {
    color: var(--color-dark);
  }
}

.slidev-layout {
  font-family: 'Inter';
  padding: 2.5rem;
  color: var(--color-dark);
}

.slidev-layout h1,
.slidev-layout h2,
.slidev-layout h3,
.slidev-layout h4 {
  font-family: 'Inter';
  color: var(--color-dark);
  font-weight: 700;
}

</style>

<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

<div class="flex justify-between items-start">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>


<div class="absolute inset-0 flex flex-col justify-center items-center">

# DTO без боли

## как class-transformer упрощает жизнь

</div>

<div class="abs-bl m-6 flex gap-2 items-center">
  <img class="w-125px h-115px rounded-[50%]" src="https://static.tildacdn.com/tild3239-3532-4537-b230-306365623135/Group_1321314990.png" />

  <div class="flex flex-col gap-2 items-start">
    <h4>Хоменко Владислав</h4><h5>Frontend Architect</h5>
  </div>
</div>

  <!-- Добрый день! Меня зовут Владислав Хоменко, я Frontend Architect. Сегодня я хочу поделиться с вами опытом работы с библиотекой class-transformer, которая кардинально упростила работу с DTO в нашем проекте.
  
  В этой презентации мы рассмотрим реальные проблемы, с которыми сталкивается каждый разработчик при работе с API, и покажу, как class-transformer помогает их решить элегантно и эффективно.
  
  Это реальный production опыт, где мы активно используем class-transformer для работы с более чем 50 моделями и 30+ API сервисами. -->
---
transition: slide-up
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

<div class="grid grid-cols-2 gap-8 pt-4 mb-6">

<div>

# 🎯 О чем поговорим
- #### Что такое DTO и зачем нужны
- #### Проблемы сериализации данных
- #### Решение проблемы
- #### Реальные примеры из проекта

</div>

</div>

<!-- DTO (Data Transfer Object) — это объект, предназначенный для передачи данных между слоями приложения. В современной разработке мы часто работаем с клиент-серверной архитектурой, где данные путешествуют между фронтендом и бэкендом.

Зачем нужны DTO?
- Типизация — одна из главных причин. Когда мы работаем с TypeScript, мы хотим иметь строгую типизацию данных. DTO позволяют нам описать структуру данных, которую мы ожидаем получить от API.
- Валидация — это следующий важный аспект. DTO могут проверять корректность данных, которые приходят с сервера. Это помогает выявлять ошибки на ранних этапах.
- Преобразование — автоматическое преобразование типов из одного формата в другой. Например, строка даты в объект Date.
- Документация — DTO служат живой документацией для команды. Глядя на класс User, мы сразу понимаем, какие данные он содержит. -->
---
transition: slide-up
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

<h1>Проблема сериализации данных</h1>

<div class="grid grid-cols-2 gap-8 pt-4 -mb-6">
<div>

### 🔴 API (Backend)
```json
{
  "id": "123",
  "createdAt": "2024-01-15T10:30:00Z",
  "avatar": {
    "id": "456",
    "name": "some image",
    "size": 2048
    ...
  }
}
```

</div>

<div>

### 🟢 Frontend
```typescript
interface User {
  id: string;
  createdAt: Date;  // ← Нужно преобразовать
  avatar: Avatar;   // ← Нужно преобразовать
}
```

</div>

</div>

<!-- Давайте разберем реальную проблему, с которой мы все сталкивались.

Когда API возвращает данные, они приходят в формате JSON. Это означает, что все значения — это простые JavaScript типы: строки, числа, объекты, массивы.

А во фронтенде мы хотим работать с типизированными классами, где createdAt должен быть объектом Date, а avatar — экземпляром класса Avatar. -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

<h1>Проблемы сериализации</h1>

<h2>Несоответствие форматов данных</h2>

<div class="p-3 bg-red-100 rounded-lg text-sm">

- 🔴 **Строки vs Объекты** - API возвращает строки дат, нужны Date объекты
- 🔴 **Вложенные структуры** - сложная логика преобразования
- 🔴 **Двунаправленность** - разные форматы для отправки и получения
- 🔴 **Ошибки типизации** - runtime ошибки из-за неправильных типов

</div>

<div v-click class="mt-4 p-3 bg-yellow-100 rounded-lg text-sm">

## Статистика проблем:
- **80% багов** связаны с неправильной сериализацией
- **30% времени разработки** тратится на преобразование данных
- **Сложность поддержки** растет с размером проекта

</div>

<!-- Проблемы несоответствия форматов:

Строки vs Объекты — API возвращает строки дат в ISO формате, но нам нужны объекты Date. Если мы попытаемся использовать строку как Date, мы получим ошибки.

Вложенные структуры — когда у нас есть сложные объекты с вложенными данными, нам нужно вручную преобразовывать каждый уровень вложенности. Это становится особенно сложно, когда у нас есть массивы объектов внутри других объектов.

Двунаправленность — это особая проблема. Когда мы получаем данные с сервера, мы хотим преобразовать их в классы. Но когда мы отправляем данные обратно, мы должны преобразовать классы обратно в JSON. И часто форматы для отправки и получения разные!

Ошибки типизации — из-за неправильных типов возникают runtime ошибки. TypeScript не может помочь нам на этапе компиляции, потому что типы не совпадают с реальными данными.

В наших проектах мы обнаружили, что 80% багов связаны с неправильной сериализацией данных, 30% времени разработки тратится на преобразование данных, и сложность поддержки растет экспоненциально с размером проекта. Это серьезные цифры, которые показывают, насколько важна правильная работа с данными. -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Проблемы с обычными объектами

## Что происходит когда мы работаем только с plain objects?

<div class="p-3 bg-red-100 rounded-lg text-sm">

- 🔴 **Нет методов** - только данные, никакой логики
- 🔴 **Нет валидации** - данные могут быть неправильными
- 🔴 **Нет вычисляемых свойств** - приходится дублировать логику
- 🔴 **Сложно тестировать** - нет четкой структуры

</div>

<div v-click class="mt-4 p-3 bg-yellow-100 rounded-lg text-sm">

## Пример проблем:
```typescript
const user = { name: "John", createdAt: "2024-01-15T10:30:00Z" };
// user.createdAt - это строка, а не Date!
// user.fullName - не существует, нужно везде писать логику
// user.isActive() - нет методов!
```

</div>

<!-- Многие разработчики используют plain objects (обычные объекты) для работы с данными. Давайте посмотрим, какие проблемы это создает.

Что происходит когда мы работаем только с plain objects?
- Нет методов — plain objects содержат только данные, никакой логики. Если нам нужна функция, которая вычисляет полное имя пользователя, нам приходится создавать отдельную функцию или дублировать логику везде.
- Нет валидации — данные могут быть неправильными, но мы об этом не узнаем, пока не попытаемся их использовать. Нет способа проверить корректность данных при создании объекта.
- Нет вычисляемых свойств — если нам нужно вычислить какое-то значение на основе других полей, мы вынуждены везде дублировать эту логику.
- Сложно тестировать — нет четкой структуры, которую можно тестировать. Мы тестируем функции, а не объекты.

Это создает огромное количество дублирования кода и потенциальных мест для ошибок. -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Ручное преобразование - это боль 😫

```typescript {all|4-13|14-16}
// ❌ Плохо: ручное преобразование
getUser(): Observable<User> {
  return this.http.get<User>('/api/user').pipe(
    map((data) => {
      return {
        id: data.id,
        name: data.name,
        createdAt: new Date(data.createdAt), // Ручное преобразование
        avatar: data.avatar ? {
          id: data.avatar.id,
          filePath: data.avatar.filePath,
          // ... много дублированного кода
        } : null
      } as User;
    })
  );
}
```

<!-- Когда мы пытаемся решить проблемы с plain objects, мы начинаем делать ручное преобразование. Давайте посмотрим, как это выглядит.

-->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Проблемы ручного подхода:


<div class="mt-4 p-3 bg-red-100 rounded-lg text-sm">

- 🔴 **Много кода** - дублирование логики преобразования
- 🔴 **Ошибки** - легко забыть преобразовать поле
- 🔴 **Поддержка** - сложно изменять структуру
- 🔴 **Типизация** - нет строгой типизации
- 🔴 **Читаемость** - много boilerplate кода
- 🔴 **Тестирование** - сложно тестировать преобразования

</div>

<div v-click class="mt-4 p-3 bg-yellow-100 rounded-lg text-sm">

## Статистика:
- **150+ строк кода** для простого CRUD
- **Дублирование логики** в каждом методе
- **Высокий риск ошибок** при изменениях

</div>

<!-- **Много кода** — нам приходится писать десятки строк кода только для того, чтобы преобразовать объект из одного формата в другой. Это дублирование логики преобразования в каждом методе.

**Ошибки** — легко забыть преобразовать какое-то поле. Или преобразовать его неправильно. Особенно когда у нас много вложенных объектов.

**Поддержка** — когда структура данных на бэкенде меняется, нам приходится изменять код преобразования в каждом месте, где мы его использовали. Это создает огромную работу по рефакторингу.

**Типизация** — мы используем `as User` для приведения типов, но TypeScript не может гарантировать, что объект действительно соответствует интерфейсу User.

**Читаемость** — код перегружен boilerplate кодом. Сложно понять, что происходит в бизнес-логике, когда она смешана с логикой преобразования.

**Тестирование** — сложно тестировать преобразования отдельно от бизнес-логики. Приходится создавать моки данных, которые имитируют ответы API. -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

<div class="flex justify-center items-center h-100">

# А что если использовать классы???

</div>

<!-- Классы решают многие проблемы, которые есть у plain objects. Давайте посмотрим, как классы улучшают ситуацию.
-->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Преимущества классов

## Почему классы лучше обычных объектов?

<div class="p-3 bg-green-100 rounded-lg text-sm">

- ✅ **Методы и логика** - можно добавить бизнес-логику
- ✅ **Вычисляемые свойства** - геттеры для сложных вычислений
- ✅ **Валидация** - конструктор и методы для проверки данных
- ✅ **Тестируемость** - четкая структура для unit тестов
- ✅ **Документация** - код самодокументируется

</div>

<!-- Преимущества классов:
- Методы и логика — в классах мы можем добавить бизнес-логику. Например, метод `isActive()` или `getAge()` могут быть частью класса User, а не отдельными функциями.
- Вычисляемые свойства — геттеры позволяют создавать вычисляемые свойства. Например, `fullName` может быть геттером, который автоматически формируется из `firstName` и `lastName`.
- Валидация — в конструкторе мы можем проверять данные и выбрасывать ошибки, если данные некорректны.
- Тестируемость — классы имеют четкую структуру, которую легко тестировать. Мы можем создать unit тесты для методов класса.
- Документация — код самодокументируется. Глядя на класс User, мы сразу видим все его свойства и методы.
  -->

---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Пример преимуществ

<div class="mt-4  rounded-lg text-sm">

```typescript
class User {
  ...
  createdAt: Date;

  constructor(..., name: string, createdAt: string) {
    this.createdAt = new Date(createdAt)
  }
  
  get fullName(): string {
    return `${this.firstName} ${this.lastName}`.trim();
  }
  
  isActive(): boolean {
    return this.createdAt > new Date('2024-01-01');
  }
}
```

</div>

<!-- Вот пример того, как классы улучшают ситуацию по сравнению с plain objects:

В классе User мы можем:
- Добавить вычисляемое свойство `fullName` через геттер, который автоматически формируется из `firstName` и `lastName`
- Добавить метод `isActive()` с бизнес-логикой, который проверяет, активен ли пользователь
- Добавить метод `getAge()` для вычисления возраста

Это гораздо лучше, чем plain objects! Но все еще есть проблема - нужно писать конструктор с ручной логикой преобразования.
 -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Первое улучшение: конструктор класса 👨‍🔧

```typescript
// ✅ Почти хорошо: с использованием class
getUser(): Observable<User> {
  return this.http.get<User>('/api/user').pipe(
    map((data) => new User(data))
  );
}
```

<!-- Когда мы начинаем использовать классы, первое что приходит в голову — использовать конструктор для преобразования данных. Это выглядит намного лучше! Всего одна строка вместо десятков. Но давайте посмотрим, что происходит внутри конструктора.
-->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-3">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Но конструктор класса - это много кода 😰

```typescript {all|14-17|18-22}
class User {
  // Примитивы - просто копируем
  id: string;
  email: string;
  
  // Даты - преобразуем вручную
  createdAt: Date;
  
  // Вложенные объекты - создаем вручную
  avatar: Avatar | null;
  roles: Role[]
  
  constructor(data: any) {
    this.id = data.id;
    this.email = data.email;
    this.createdAt = new Date(data.createdAt); // ❌ Ручное преобразование
    this.avatar = data.avatar ? new Avatar(data.avatar) : null; // ❌ Вручную проверяем и создаем
    this.roles = data.roles.map((role) => new Role(role)); // ❌ Ручное преобразование
  }
}
```

<!-- К сожалению, конструктор все еще требует много кода для правильного преобразования всех полей. Проблемы все еще остаются:
- Много кода в конструкторе
- Дублирование логики для каждого типа поля
- Легко забыть обработать какое-то поле
- При изменении структуры данных нужно менять конструктор

И самое главное — это работает только в одну сторону (от JSON к классу). А что делать, когда нужно отправить данные обратно на сервер?  -->

---
layout: image
class: text-center
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

<div class="flex flex-col justify-center items-center h-100">

# Знакомьтесь: class-transformer 🎯
<div class="text-2xl mt-8">
TypeScript библиотека для автоматического<br>преобразования объектов в классы и обратно
</div>

</div>

<!-- И вот здесь на сцену выходит class-transformer — библиотека от TypeStack, той же команды, что создала `class-validator`, `routing-controllers`, `typedi`.
 -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Что такое class-transformer?

<div class="mt-12 space-y-6">

<div v-click>

## 📦 Библиотека от TypeStack
##### Та же команда, что создала `class-validator`, `routing-controllers`, `typedi`

</div>

<div v-click>

## 🎯 Основная задача
##### Преобразовать plain JavaScript объекты (JSON) в экземпляры TypeScript классов **автоматически**

</div>

<div v-click>

## 🔄 Работает в обе стороны
##### - `plainToInstance()` - JSON → Class
##### - `instanceToPlain()` - Class → JSON

</div>

</div>

<!-- Что такое class-transformer?
- Основная задача class-transformer — преобразовать plain JavaScript объекты (JSON) в экземпляры TypeScript классов автоматически, без написания конструкторов с ручной логикой.

Ключевые особенности:
- Работает в обе стороны: `plainToInstance()` - преобразует JSON → Class, `instanceToPlain()` - преобразует Class → JSON
- Декларативный подход — мы описываем, КАК преобразовывать данные, используя декораторы, а не пишем код преобразования вручную.
- TypeScript-first — библиотека разработана специально для TypeScript и полностью поддерживает его типизацию.
- Производительность — библиотека оптимизирована и работает быстро даже с большими объемами данных. -->

---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Основные декораторы class-transformer

### 1️⃣ @Type() - преобразование типов

```typescript
export class User {
  @Type(() => Date)
  createdAt: Date;  // Строка → Date

  @Type(() => Avatar)
  avatar: Avatar;   // Plain object → Avatar

  @Type(() => Role)
  role: Roles[];   // Plain objects → Role - Работает с массивами
}
```

<!-- Давайте разберем основные декораторы, которые предоставляет class-transformer.

Самый важный декоратор — `@Type()`. Он говорит class-transformer, какой тип нужно использовать для преобразования поля.

Как это работает:
- Когда мы вызываем `plainToInstance(User, jsonData)`, class-transformer видит декоратор `@Type(() => Date)` и автоматически преобразует строку даты в объект Date.
- Для вложенных объектов он создает экземпляры классов, используя указанный тип.
- Для массивов он преобразует каждый элемент массива в экземпляр указанного класса.

Важно: мы передаем функцию `() => Date`, а не просто `Date`, потому что это нужно для решения проблемы циклических зависимостей в TypeScript.
 -->

---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Основные декораторы class-transformer

### 2️⃣ @Expose() - управление сериализацией

```typescript
export class User {
  @Expose({ name: 'date', toClassOnly: true })
  createdAt: Avatar;  // При сериализации → date

  @Expose({ name: 'avatarId', toPlainOnly: true })
  ....
  avatar: Avatar;  // При сериализации → avatarId

  @Expose({ toPlainOnly: true }) 
  get getFileName(): string { // Работает с геттерами
    return this.avatar.name;
  };  
}
```
<!-- Декоратор `@Expose()` позволяет управлять тем, какие поля сериализуются и как они называются в JSON.

Опции:
- `toClassOnly: true` — поле используется только при преобразовании JSON → Class
- `toPlainOnly: true` — поле используется только при преобразовании Class → JSON
- `name: 'fieldName'` — альтернативное имя поля в JSON

Геттеры тоже могут быть сериализованы с помощью `@Expose({ toPlainOnly: true })`.
 -->

---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Основные декораторы class-transformer

### 3️⃣ @Transform() - преобразования

```typescript
export class User {
  @Expose({ name: 'avatarId', toPlainOnly: true })
  @Transform((params) => params.value ? toId(params.value) : null, { toPlainOnly: true })
  avatar: Avatar;  // Avatar → string ID

  ...
}
```
<!-- Декоратор `@Transform()` позволяет выполнять произвольные преобразования значений.

Как это работает:
- Первый параметр — функция преобразования. Она получает объект `params` с полями: `value` — текущее значение поля, `key` — имя поля, `obj` — весь объект, `type` — тип класса.
- Второй параметр — опции (например, `toPlainOnly: true`)

Это позволяет выполнять сложные преобразования, например, преобразовать объект Avatar в строку ID при отправке на сервер.
 -->

---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Основные декораторы class-transformer

### 4️⃣ @Exclude() - исключение полей

```typescript
export class User {
  @Exclude({ toPlainOnly: true })
  internalField: string; // Не отправляется на бэкенд

  ...
}
```
<!-- Декоратор `@Exclude()` позволяет исключать поля из сериализации.

Опции:
- `toPlainOnly: true` — поле не отправляется на бэкенд
- `toClassOnly: true` — поле не используется при создании объекта из JSON

Это полезно для внутренних полей, которые не должны попадать в API запросы, или для полей, которые приходят только с сервера и не нужны на фронтенде.
 -->
 
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Как это работает?

<div class="mt-4">

### 1️⃣ Описываем класс с декораторами

```typescript
class User {
  id: string;
  
  @Expose({ name: 'avatarId', toPlainOnly: true }) // Автоматически сериализует имя при отправке на бэкенд
  @Transform(toId, { toPlainOnly: true }) // ← Автоматически преобразует в id при отправке на бэкенд
  @Type(() => Avatar)  // ← Автоматически создаст Avatar
  avatar: Avatar;
  
  @Type(() => Role)  // ← Работает и с массивами!
  roles: Role[];
  
  // Методы работают как обычно
  getAge(): number {
    return new Date().getFullYear() - this.createdAt.getFullYear();
  }
}
```

</div>

<!-- 
Давайте посмотрим на полный пример того, как все это работает вместе.

Шаг 1: Описываем класс с декораторами. Мы используем комбинацию декораторов для сложного преобразования:
- `@Type(() => Date)` указывает тип для преобразования даты
- `@Expose({ name: 'avatarId', toPlainOnly: true })` автоматически сериализует имя при отправке на бэкенд
- `@Transform()` автоматически преобразует в id при отправке на бэкенд
- `@Type(() => Avatar)` автоматически создаст Avatar при получении
- `@Type(() => Role)` работает и с массивами!

Методы работают как обычно, вычисляемые свойства тоже работают. --> 
 
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-4">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Как это работает?

<div class="mt-4">

### 2️⃣ Преобразуем JSON в класс одной строкой

```typescript
// До
const response = await fetch('/api/user');
const user = new User(response.json()); // ❌ Нужен конструктор с логикой

// После
const response = await fetch('/api/user');
const user = plainToInstance(User, response.json()); // ✅ Всё автоматически!
```

</div>

<div v-click class="mt-8 p-4 bg-blue-100 rounded-lg">

### 🎁 Что получаем?
- `user.createdAt` - настоящий `Date`
- `user.avatar` - экземпляр `Avatar` с методами
- `user.roles` - массив экземпляров `Role`
- `user.getAge()` - все методы класса работают

</div>

<!-- Шаг 2: Преобразуем JSON в класс одной строкой.

До: нам нужен конструктор с логикой преобразования.
После: просто вызываем `plainToInstance(User, jsonData)` и всё автоматически!

Что мы получаем после вызова `plainToInstance()`:
- `user.createdAt` — это настоящий объект `Date`, а не строка. Мы можем вызывать методы типа `getTime()`, `toISOString()` и т.д.
- `user.avatar` — это экземпляр класса `Avatar` с его методами и геттерами.
- `user.roles` — это массив экземпляров класса `Role`, а не массив plain objects.
- `user.getAge()` — все методы класса работают корректно, потому что все поля преобразованы правильно.

Это магия! Но настоящая магия начинается, когда мы начинаем использовать кастомные декораторы.
 -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Решение с class-transformer

## Автоматическое преобразование ✨

```typescript {all|8-12}
// ✅ Хорошо: с class-transformer
@Injectable({
  providedIn: 'root'
})
export class UserApiService {
  constructor(private http: HttpClient) {}

  getUser(): Observable<User> {
    return this.http.get('/api/user').pipe(
      map((data: any) => plainToInstance(User, data)) // Автоматическое преобразование!
    );
  }
}
```

<!-- Вот как выглядит решение с class-transformer. Мы просто вызываем `plainToInstance(User, data)` вместо написания конструктора с ручной логикой.

Это автоматическое преобразование! Все декораторы, которые мы добавили в класс User, применяются автоматически.
 -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Решение: class-transformer ✨

<div class="p-3 bg-green-100 rounded-lg text-sm mt-6">

- ✅ **Автоматическое преобразование** - JSON → классы без лишнего кода
- ✅ **Декораторы** - декларативное описание преобразований
- ✅ **Type safety** - полная поддержка TypeScript
- ✅ **Двунаправленность** - классы ↔ JSON
- ✅ **Производительность** - быстрая работа

</div>

<!-- Решение с class-transformer предоставляет:
- Автоматическое преобразование - JSON → классы без лишнего кода
- Декораторы - декларативное описание преобразований
- Type safety - полная поддержка TypeScript
- Двунаправленность - классы ↔ JSON
- Производительность - быстрая работа

Но все еще нужно писать `map((data: any) => plainToInstance(User, data))` в каждом методе. Что-то хочется с этим сделать...
 -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Решение с class-transformer

## Автоматическое преобразование ✨

```typescript {8-12}
// ✅ Хорошо: с class-transformer
@Injectable({
  providedIn: 'root'
})
export class UserApiService {
  constructor(private http: HttpClient) {}

  getUser(): Observable<User> {
    return this.http.get('/api/user').pipe(
      map((data: any) => plainToInstance(User, data)) // Что-то хочется с этим сделать....
    );
  }
}
```
<!-- В реальных проектах мы не хотим каждый раз писать `plainToInstance()` в каждом методе API сервиса. Мы создали кастомные декораторы, которые автоматически применяют преобразование.
 -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

<img src="https://sticker-collection.com/stickers/plain/Dichset/512/aec2c11d-826c-430b-8623-56cc72400a01file_3836920.webp" alt="class-transformer magic" style="margin: 24px auto; display: block;" />

---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Кастомные декораторы MapTo и MapListTo

## Автоматическое преобразование ✨

```typescript {all|7-10}
@Injectable({
  providedIn: 'root'
})
export class UserApiService {
  constructor(private http: HttpClient) {}

  @MapTo(User)
  getUser(): Observable<User> {
    return this.http.get('/api/user');
  }
}
```

<!-- Теперь нам не нужно писать `map((data) => plainToInstance(User, data))` в каждом методе! Достаточно просто добавить декоратор `@MapTo(User)` перед методом, и преобразование произойдет автоматически. -->

---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# MapTo - для одиночных объектов

```typescript {all|10-14}
export const MapTo =
  <ItemType extends object>(targetClass: Constructor<ItemType>) =>
  <MethodType extends (...args: any[]) => Observable<ItemType>>(
    _target: any,
    _methodName: string | symbol,
    descriptor: TypedPropertyDescriptor<MethodType>,
  ): TypedPropertyDescriptor<MethodType> => {
    const originalMethod = descriptor.value;

    descriptor.value = function (this: any, ...args: any[]) {
      const toInstance = (dto: ItemType): ItemType => plainToInstance(targetClass, dto);
      return originalMethod?.apply(this, args).pipe(map(toInstance));
    } as MethodType;

    return descriptor;
  };
```
<!-- MapTo - для одиночных объектов.

Как это работает:
1. Декоратор принимает класс (`targetClass`), в который нужно преобразовать данные.
2. Он перехватывает метод API сервиса.
3. При вызове метода он получает результат (Observable с данными).
4. Преобразует данные с помощью `plainToInstance()`.
5. Возвращает Observable с преобразованными данными.

Теперь нам не нужно писать `map((data) => plainToInstance(User, data))` в каждом методе!
 -->

---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# MapListTo - для массивов

```typescript {all|10-13}
export const MapListTo =
  <ItemType>(ItemClass: Constructor<ItemType>) =>
  <MethodType extends (...args: any[]) => Observable<EntityList<ItemType> | ItemType[]>>(
    _target: any,
    _methodName: string | symbol,
    descriptor: TypedPropertyDescriptor<MethodType>,
  ): TypedPropertyDescriptor<MethodType> => {
    const originalMethod = descriptor.value;

    descriptor.value = function (this: any, ...args: any[]) {
      return originalMethod?.apply(this, args).pipe(map((item) => plainToInstance(ItemClass, data)));
    } as MethodType;

    return descriptor;
  };
```
<!-- MapListTo - для массивов.

Для работы с массивами и EntityList (пагинация) мы создали аналогичный декоратор `MapListTo`.

Этот декоратор умеет работать как с обычными массивами, так и с объектами типа `EntityList`, которые содержат пагинацию.

Он автоматически преобразует каждый элемент массива в экземпляр указанного класса, используя `plainToInstance()`.
 -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-4">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Helper mapInstanceToPlain

## Проблема: отправка данных на бэкенд

```typescript {all|2-6|9-15}
// ❌ Проблема: как отправить User на бэкенд?
updateUser(user: Partial<User>): Observable<User> {
  // user.avatar - это объект Avatar, но бэкенд ожидает avatarId: string
  return this.http.patch('/api/user', user); // ❌ Неправильно!
}

// ❌ Ручное преобразование
updateUser(user: User): Observable<User> {
  const payload = {
    id: user.id,
    name: user.name,
    email: user.email,
    avatarId: user.avatar?.id || null, // Ручное преобразование
    createdAt: user.createdAt?.toISOString() // Ручное преобразование
  };
  return this.http.patch('/api/user', payload);
}
```

<!-- Одна из самых сложных задач — это отправка данных обратно на сервер. Когда у нас есть экземпляр класса `User`, а бэкенд ожидает JSON с другими именами полей.

Мы создали helper функцию `mapInstanceToPlain`, которая автоматически применяет все декораторы при преобразовании класса в JSON:
- Сначала мы создаем правильный экземпляр класса из переданного объекта (чтобы применить все декораторы `@Type`, `@Expose`, `@Transform`).
- Затем преобразуем экземпляр класса обратно в plain object, применяя все декораторы с `toPlainOnly: true`.

Теперь все преобразования выполняются автоматически, учитывая декораторы `@Expose({ name: 'avatarId', toPlainOnly: true })` и `@Transform()`.
 -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-2">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# UsersApiService - реальный пример

```typescript {all|5-8|9-13|14-19}
@Injectable({ providedIn: 'root' })
export class UsersApiService {
  readonly #apiService = inject(ApiService);

  @MapListTo(User)
  getUserList(params?: ListQueryParams<User>): Observable<User[]> {
    return this.#apiService.getList<User>(USERS_BASE_PATH, params);
  }

  @MapTo(User)
  getProfile(): Observable<User> {
    return this.#apiService.get<User>(USERS_PROFILE_PATH);
  }

  @MapTo(User)
  updateUser(user: Partial<User>): Observable<User> {
    const transformedUser = mapInstanceToPlain(User, user);
    return this.#apiService.patch<User>(USERS_PROFILE_PATH, transformedUser);
  }
}
```

<!-- Давайте посмотрим на реальный пример из нашего production проекта.

Что мы видим:
- Код очень чистый и читаемый.
- Вся логика преобразования скрыта в декораторах.
- Методы содержат только бизнес-логику, без boilerplate кода.
 -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Преимущества использования декораторов

<div class="p-3 bg-green-100 rounded-lg text-sm">

- 🟢 **Чистый код** - минимум boilerplate
- 🟢 **Автоматика** - преобразование происходит автоматически
- 🟢 **Типизация** - полная поддержка TypeScript
- 🟢 **Переиспользование** - один декоратор для всех методов
- 🟢 **Читаемость** - декларативный подход
- 🟢 **Тестирование** - легко тестировать сервис

</div>

<div v-click class="mt-4 p-3 bg-blue-100 rounded-lg text-sm">

### 🚀 Результат

- **В 7 раз меньше кода** в API сервисах
- **Строгая типизация** всех данных
- **Простая поддержка** и изменения
- **Отличный DX** для разработчиков

</div>

<!-- Давайте подведем итоги преимуществ:

- Чистый код — минимум boilerplate кода. Вся логика преобразования описывается декларативно через декораторы.
- Автоматика — преобразование происходит автоматически. Нам не нужно помнить о преобразовании в каждом методе.
- Типизация — полная поддержка TypeScript. Все типы проверяются на этапе компиляции.
- Переиспользование — один декоратор `@MapTo` или `@MapListTo` можно использовать для всех методов API сервиса.
- Читаемость — код читается как документация. Видя декоратор `@MapTo(User)`, сразу понятно, что метод возвращает экземпляр класса User.
- Тестирование — легко тестировать сервис. Преобразование данных происходит автоматически, нам нужно тестировать только бизнес-логику.

Результат: В 7 раз меньше кода в API сервисах, строгая типизация всех данных, простая поддержка и изменения, отличный DX (Developer Experience) для разработчиков.
 -->
---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

# Лучшие практики

<div class="grid grid-cols-2 gap-6">

<div>

## ✅ Что делать
1. **Используйте декораторы** - @Type, @Expose, @Transform
2. **Создавайте кастомные декораторы** - MapTo, MapListTo
4. **Тестируйте преобразования** - unit тесты для моделей
5. **Используйте геттеры** - для вычисляемых свойств

</div>

<div>

## ❌ Чего избегать
1. **Ручное преобразование** - используйте class-transformer
2. **any типы** - строгая типизация везде
3. **Дублирование кода** - переиспользуйте декораторы
4. **Игнорирование ошибок** - обрабатывайте исключения

</div>

</div>

<!-- На основе опыта работы с class-transformer в production проектах, вот несколько рекомендаций.

Что делать:
- Используйте декораторы везде. Не пренебрегайте декораторами. Даже если кажется, что можно обойтись без них, использование декораторов делает код более предсказуемым и легко изменяемым.
- Создавайте кастомные декораторы. Если вы видите повторяющийся паттерн преобразования, создайте свой декоратор. Это значительно упростит код в будущем.
- Тестируйте преобразования. Создавайте unit тесты для моделей с декораторами. Проверяйте, что преобразования работают корректно в обе стороны.
- Используйте геттеры. Геттеры в классах — это отличный способ создать вычисляемые свойства. Они работают с class-transformer и могут быть сериализованы с помощью `@Expose({ toPlainOnly: true })`.
- Документируйте сложные преобразования. Если у вас есть сложная логика в `@Transform()`, добавьте JSDoc комментарий, объясняющий, что происходит.

Чего избегать:
- Ручное преобразование. Если вы используете class-transformer, не смешивайте ручное преобразование с автоматическим. Это создает непредсказуемое поведение.
- any типы. Избегайте `any` типов. Используйте строгую типизацию везде. Это поможет class-transformer правильно преобразовать данные.
- Дублирование кода. Если вы видите дублирование логики преобразования в разных местах, создайте общий декоратор или helper функцию.
- Игнорирование ошибок. Обрабатывайте исключения при преобразовании. Если данные некорректны, лучше упасть с понятной ошибкой, чем молча получить неправильный результат.
- Смешивание логики. Модели должны содержать только данные и методы работы с данными. Бизнес-логику лучше выносить в сервисы.
 -->

---
transition: slide-up
level: 2
layout: image
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>
<div>

# 📌 Что мы узнали

1. **DTO важны** — они обеспечивают типизацию, валидацию и преобразование данных.
2. **class-transformer упрощает** — автоматическое преобразование без написания большого количества кода.
3. **Кастомные декораторы** — `MapTo` и `MapListTo` делают использование class-transformer еще удобнее.
4. **Двунаправленность** — мы можем преобразовывать данные в обе стороны (JSON ↔ Class) с помощью `toPlainOnly` и `toClassOnly`.
5. **Реальные примеры** — все это работает в production проектах и значительно упрощает разработку.

</div>



<!-- Давайте подведем итоги того, что мы узнали.

Ключевые моменты:
1. DTO важны — они обеспечивают типизацию, валидацию и преобразование данных.
2. class-transformer упрощает — автоматическое преобразование без написания большого количества кода.
3. Кастомные декораторы — `MapTo` и `MapListTo` делают использование class-transformer еще удобнее.
4. Двунаправленность — мы можем преобразовывать данные в обе стороны (JSON ↔ Class) с помощью `toPlainOnly` и `toClassOnly`.
5. Реальные примеры — все это работает в production проектах и значительно упрощает разработку.

Результаты использования:
- Меньше кода — автоматическое преобразование снижает объем кода в 7 раз
- Больше безопасности — строгая типизация предотвращает ошибки
- Легче поддержка — декларативный подход упрощает изменения
- Лучше DX — отличный developer experience, приятно работать с таким кодом

Следующие шаги:
1. Попробуйте class-transformer в своем проекте
2. Начните с простых моделей
3. Создайте свои кастомные декораторы для повторяющихся паттернов
4. Поделитесь опытом с сообществом

Спасибо за внимание! Теперь готов ответить на ваши вопросы.
 -->
---
layout: image
class: text-center
image: /backgrounds/media-image1.png
---

<div class="flex justify-between items-start mb-6">
  <img src="/backgrounds/media-image4.png" class="h-15" alt="Левое изображение" />
  <img src="/backgrounds/media-image2.png" class="h-10" alt="Правое изображение" />
</div>

<div class="flex flex-col justify-center items-center h-100">

# Спасибо за внимание!

## Вопросы и обсуждение

<div class="pt-8 text-sm opacity-50">
  <div>Полезные ссылки:</div>
  <div class="flex justify-center gap-4 mt-2">
    <a href="https://github.com/typestack/class-transformer" target="_blank">class-transformer</a>
    <a href="https://www.typescriptlang.org/docs/handbook/decorators.html" target="_blank">TypeScript Decorators</a>
  </div>
</div>

</div>


